export enum RoleEnum {
  ADMIN = "admin",
  INSTITUTION = "institution",
  INSTRUCTOR = "instructor",
  LEARNER = "learner",
}

export enum UserType {
  ADMIN = "admin",
  INSTITUTION = "institution",
  INSTRUCTOR = "instructor",
  LEARNER = "learner",
}
